fastapi==0.111.0
uvicorn[standard]==0.30.1
pydantic==2.7.4
pydantic-settings==2.3.4
sqlalchemy==2.0.31
python-multipart==0.0.9
gitpython==3.1.43
networkx==3.3

# ONNX Runtime (via fastembed) instead of torch/sentence-transformers: torch
# alone typically costs 300-500MB of resident memory just from being
# imported, which repeatedly OOM-killed the scan job on Render's free tier
# (512MB) for any non-trivial repo. fastembed runs the same embedding model
# through ONNX Runtime's CPU provider, with a much smaller memory footprint.
fastembed==0.8.0
qdrant-client==1.10.1
rank-bm25==0.2.2
tree-sitter==0.21.3
tree-sitter-languages==1.10.2
radon==6.0.1
openai==1.35.13
python-dotenv==1.0.1
httpx==0.27.0
pytest==8.2.2
pytest-asyncio==0.23.7
